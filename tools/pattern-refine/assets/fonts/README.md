# Fonts

此目录用于存放项目明确依赖的字体文件。

当前策略：

- MVP 线稿输出不要求中文文字层，因此字体不是 MVP 阻塞项。
- 如果后续 PDF 输出包含中文文字，优先使用本目录中的 `NotoSansSC-VF.ttf`。
- 字体来源：`C:\Windows\Fonts\NotoSansSC-VF.ttf`。
- 不要在代码中直接依赖系统字体路径；运行时引用本目录中的字体文件。
- PDF 输出中文文字时必须显式嵌入字体。
